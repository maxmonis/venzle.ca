{
  "devDependencies": {
    "prettier": "^3.6.2",
    "prettier-plugin-organize-imports": "^4.2.0",
    "typescript": "~5.8.3",
    "vite": "^7.1.2"
  },
  "name": "venn",
  "private": true,
  "scripts": {
    "build": "tsc && vite build",
    "dev": "vite --host",
    "lint": "prettier --write --ignore-path .gitignore .",
    "prod": "npm run build && vite preview --host"
  },
  "type": "module",
  "version": "0.0.0"
}
